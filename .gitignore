# Node
node_modules/

# Android (build output and local settings)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/app/src/main/assets/public/

# Signing keys never belong in the repository
*.jks
*.keystore
android/app/src/main/res/xml/config.xml
*.apk
*.aab

# Android Studio / IntelliJ
.idea/
*.iml

# Source PDFs for the built-in plans. Kept locally as reference, never
# committed: the repo is public and these are someone else's paid material.
programs/

# System
.DS_Store
Thumbs.db
